"""Infrastructure implementations of the domain repository ports.

These concrete implementations translate between ORM (`app.models`) and the
application layer. They depend only on the abstract interfaces defined in
`app.domain.repositories`, keeping the domain pure.
"""
