# Frontend environment template. Copy to `frontend/.env.local` (Next.js loads
# `.env.local` automatically). NEVER commit real secrets.

# Base URL of the backend API (without the /api/v1 suffix).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
