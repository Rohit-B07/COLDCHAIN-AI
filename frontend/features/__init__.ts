/**
 * Feature modules.
 *
 * Each business capability lives in its own folder and exposes a public
 * barrel `index.ts`. Features are self-contained: their components, hooks,
 * and API-call functions stay inside the folder, enforcing encapsulation and
 * making it trivial to add, remove, or replace a capability.
 */
